{
  "name": "homeshare-v2",
  "version": "1.0.0",
  "description": "Multi-Chain Real Estate Crowdfunding Platform",
  "private": true,
  "author": "Brickt Team",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "dev:full": "bash -lc 'pnpm --parallel -r dev & pnpm --filter @homeshare/backend run process:intents:watch:runtime & pnpm --filter @homeshare/backend agent:run; wait'",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean && rm -rf node_modules",
    "install:all": "pnpm install",
    "sync:testnet-addresses": "node scripts/sync-testnet-addresses.mjs"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "dependencies": {
    "cloudinary": "^2.9.0",
    "type-check": "^0.4.0"
  }
}
